# 1 项目概述与赛题解读（20260305）

## 1.1 赛题硬约束（摘录到工程口径）

来自 `problem.md` 的 baseline 关键约束：

- 固定规模：`S=256, d=64, batch=1, head=1`
- 数据格式：`Q/K/V/O = Q8.8`
- 算法约束：在线 softmax、K/V tiling、不显式存 `SxS` score/p
- 接口：AXI4-Lite 配置 + DMA 读写（Q/K/V 读、O 写）
- 验收门限：
  - `MAE <= 0.03`
  - `MaxAE <= 0.10`
- 性能目标：`cycles < 300k`

## 1.2 本工程当前完成度

当前版本已完成：

- RTL 主流程闭环（Q/K/V 加载、tile compute、normalize、O 写回）
- P0 三项目标（控制路径压缩、K/V 预取重叠、数值稳定守护）
- C++ Verilator TB 端到端统计导出

当前关键结果（`docs/data/20260304_rtl_summary.csv`）：

- `total_cycles = 145,584`
- `rtl_fp32_mae = 0.000971925`
- `rtl_fp32_maxae = 0.00203197`

结论：已满足 baseline 赛题门限并有余量。

## 1.3 本报告写作边界

本报告聚焦“当前工程可复现状态”，不复述已废弃的早期架构方案；所有结论均绑定可复现命令和数据路径。
